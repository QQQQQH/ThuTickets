<import src="/pages/template/eventCard/eventCard.wxml" />
<block wx:if='{{gotEventList == 0}}'>
  空空如也
</block>
<block wx:else>
  <view wx:for='{{eventList}}' wx:key='index'>
    <template is='eventCard' data='{{...item}}' />
  </view>
</block>